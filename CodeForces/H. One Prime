#include<iostream>
#include<cmath>
using namespace std;
int main()
{
	long n, i;
	cin>>n;
	if(n<2)
		cout<<"NO\n";
	else{
		for(i=2;i<n;i++)
			if(n%i==0)
				break;
		if(i==n)
			cout<<"YES\n";
		else
			cout<<"NO\n";
	}
}
