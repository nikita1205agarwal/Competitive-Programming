#include <iostream>
#include <string>
using namespace std;

int main() {
    int n, freq[26]={0};
    cin>>n;

    string s;
    cin>>s;
    
    for(int i=0;i<n;i++)
    	freq[s[i]-'a']++;
    
    for(int i=0;i<26;i++)
        if(freq[i]!=0)
            cout<<freq[i]<<"\n";
    return 0;
}
