#include <iostream>
using namespace std;

int main() {
    int N, M;
    cin >> N >> M;

    int A[100001] = {0};
    int num;

    for (int i = 0; i < N; i++) {
        cin >> num;
        A[num]++;
    }

    for (int i = 1; i <= M; i++) {
        cout << A[i] << '\n';
    }

    return 0;
}
