#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double R, S;
    cin >> R >> S;

    double diagonal_of_square = sqrt(2) * S;
    
    if (diagonal_of_square <= 2 * R)
        cout << "Circle" << "\n";
    else if (S >= 2*R)
        cout << "Square" << "\n";
    else
        cout << "Complex" << "\n";

    return 0;
}
