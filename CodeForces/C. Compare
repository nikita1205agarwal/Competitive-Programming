#include <iostream>
using namespace std;

int main() {
    string X, Y;

    cin >> X >> Y;

    int result = X.compare(Y);

    if (result < 0) {
        cout << X << '\n';
    } else 
        cout << Y << '\n';
    
    return 0;
}
