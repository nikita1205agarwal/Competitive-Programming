#include <iostream>
using namespace std;

bool isPowerOfTwo(long long N) {
    if (N <= 0) return false;
    return (N & (N - 1)) == 0;
}

int main() {
    long long N;
    cin >> N;

    if (isPowerOfTwo(N)) {
        cout << "YES\n";
    } else {
        cout << "NO\n";
    }

    return 0;
}
