#include <iostream>
#include <string>
using namespace std;

int main() {
    int n;
    cin>>n;

    string s;
    cin>>s;

    string res="";
    res=res+s[0];
    for(int i=1;i<n;i++)
    	if(res[i-1]!=s[i])
    		res+=s[i];
    cout<<res.size()<<"\n";
    return 0;
}
